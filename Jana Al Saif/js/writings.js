const lightbox=document.getElementById('lightbox'),image=document.getElementById('lightbox-image'),title=document.getElementById('lightbox-title'),closeButton=document.querySelector('.lightbox-close');
function openWriting(card){image.src=`../images/${card.dataset.image}`;image.alt=card.dataset.title;title.textContent=card.dataset.title;lightbox.classList.add('open');lightbox.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';closeButton.focus()}
function closeWriting(){lightbox.classList.remove('open');lightbox.setAttribute('aria-hidden','true');document.body.style.overflow='';image.src=''}
document.querySelectorAll('.writing-card').forEach(card=>{card.addEventListener('click',()=>openWriting(card));card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openWriting(card)}})});closeButton.addEventListener('click',closeWriting);lightbox.addEventListener('click',e=>{if(e.target===lightbox)closeWriting()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&lightbox.classList.contains('open'))closeWriting()});
